---

 - name: "[ENTERPRISE-DEMO][AWS][DEPLOY-AWS] Add ec2-instance to the inv"
   hosts: localhost
   connection: local
   remote_user: "{{ ec2_remote_user }}"
   vars_files:
     - "vault/aws_vault.yml"
   roles:
     - { role: provision-aws }
